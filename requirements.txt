# The Talking Heads - Python Dependencies

# Audio Processing
pydub>=0.25.1
ffmpeg-python>=0.2.0

# TTS Providers (Local GPU)
coqui-tts>=0.27.0
bark>=0.1.0
eng-to-ipa>=0.0.2

# Machine Learning
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Image/Video Processing
Pillow>=10.0.0
opencv-python>=4.8.0

# Utilities
pyyaml>=6.0
requests>=2.31.0
python-dotenv>=1.0.0

# Web Framework (for webhooks)
flask>=3.0.0
flask-cors>=4.0.0

# Optional: External API providers (if using cloud services)
# elevenlabs>=0.2.0  # Uncomment if using ElevenLabs
# azure-cognitiveservices-speech>=1.32.0  # Uncomment if using Azure Speech

# Audio2Face Integration (NVIDIA Omniverse)
py-audio2face>=0.1.0  # Python client for Audio2Face (from GitHub)

# USD Format Support (for 3DDFA + Audio2Face workflow)
usd-core>=25.0  # USD Python API for mesh conversion